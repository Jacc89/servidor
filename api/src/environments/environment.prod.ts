export const environment = {
  production: true,
  articleUrl: "http://server3.azlogica.com:3000"
};
// Cuando ejecutas el comando ng build se usa la variable de entorno Environment pero si le agregas el comando --prod se usa esta variable de entorno. aca te dejo el comentario para que no lo olvides.
